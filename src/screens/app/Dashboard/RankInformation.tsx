import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const RankInformation = () => {
  return (
    <View>
      <Text>RankInformation</Text>
    </View>
  )
}

export default React.memo(RankInformation)

const styles = StyleSheet.create({})